[<AutoOpen>]
module Operations
let subsetOP x = subset_m (subset_s) x 
let supersetOP x = superset_m (superset_s) x 
let unionOP x = union_m (union_s) x 
let intersectOP x = intersect_m (intersect_s) x 
